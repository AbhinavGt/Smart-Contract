# Precision loss
Integer division truncates, and multiplying after dividing can discard significant value. Attackers may exploit rounding in exchanges or share accounting. Choose operation order carefully, use fixed-point helpers, and define rounding direction explicitly.
