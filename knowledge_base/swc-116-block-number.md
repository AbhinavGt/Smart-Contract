# Block number as time or randomness
Block numbers vary with network conditions and are not a reliable wall clock or unpredictable random source. Protocol logic that assumes a fixed block interval can drift. Use explicit elapsed-time tolerances and secure randomness where needed.
