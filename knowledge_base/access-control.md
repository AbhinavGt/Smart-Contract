# Missing access control
State-changing administrative functions must restrict callers and validate privileged transitions. Missing modifiers, public initializers, or an owner setter without authorization can let anyone seize control or drain funds. Define roles, protect every privileged entry point, and test unauthorized calls.
